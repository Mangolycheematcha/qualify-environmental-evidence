# Step 2B V4 Runtime Freeze

Status: **IMPLEMENTED AND TESTED OFFLINE; PENDING NEW THREE-WAY APPROVAL**

## Identities

- Policy ID: `DEMO_QUALIFICATION_POLICY_EOP101132_V4`
- Policy SHA-256: `3412570f327f4c55184ced99948f3625e718e19e994732ec204cb7dea16318dd`
- Runtime-spec ID: `EOP101132_STEP2B_V4_RUNTIME_V1`
- Runtime-spec SHA-256: `3ac7684ed5dad74428d03496a41f676a125c12be1f7930aad3121cc05a5054cb`
- Contract: `0.5.0`

The runtime-spec hash is external to the hashed document and is generated by `scripts/freeze_v4_runtime_spec.py`. The Git commit is also external so neither identity is self-referential. Human approval must bind the policy hash, runtime-spec hash, and commit together.

## Frozen Execution

The runtime verifies the clean Git baseline, exact detached approval, policy bytes, runtime-spec bytes, implementation-file hashes, CPython version, and exact NumPy/Rasterio versions before network access.

The live sequence is:

1. retrieve and verify CER project and CEA sources;
2. complete both frozen STAC searches without a cloud predicate;
3. apply the 200-item metadata inventory limit;
4. fetch signed product/granule metadata without persisting credentials;
5. resolve representations, metadata admissibility, and independent acquisitions;
6. apply the 40-acquisition gate before raster pixels;
7. process all admissible groups on the frozen AOI grid;
8. calculate coverage, NDVI aggregation, primary disposition, and separate sensitivities;
9. seal the canonical assessment;
10. recompute NDVI and disposition from unsigned AOI-level B04/B08/SCL caches in explicit offline replay mode;
11. validate provenance, scan outputs, and write checksums.

Any implementation or deterministic processing failure is preserved and cannot resume under the same run ID. The runtime does not start Step 3.
